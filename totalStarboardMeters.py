def getStarboardMeters(workoutsData, peopleData):
    starboardMeters = 0
    for workout in workoutsData:
        if workout.person.side = starboard
            starboardMeters += workout['scored_meters']
    return starBoardMeters
